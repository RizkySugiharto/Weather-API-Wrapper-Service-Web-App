{
  "name": "weather-api-wrapper-service-web-app",
  "version": "1.0.0",
  "description": "Root",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run serve\"",
    "dev-local": "concurrently \"cd backend && npm run dev-local\" \"cd frontend && npm run serve\""
  },
  "author": "Mr. Ryto",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
